The screening is complete and validated:

- **217 records processed** — matches input exactly, no duplicates, all fields present
- **13 include** — RCTs of FMT for rCDI with clinical outcomes
- **3 uncertain** — abstracts lacking sufficient detail for a confident decision (sent to human review)
- **201 exclude** — reviews, non-RCTs, non-rCDI indications, or other criteria failures

Output written to `data/screening_decisions.jsonl`.
